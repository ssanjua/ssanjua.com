---
const { class: className = '', ...rest } = Astro.props;
---

<img        
    {...rest}
    class={`rounded-lg transition-transform duration-300 hover:scale-105 ${className}`}
    src="perfil2024.gif"
    alt="ssanjua profile picture"
/>