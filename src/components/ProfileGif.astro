---

const { class: className = '', ...rest } = Astro.props

---

<img        
    {...rest}
    class={`transition-transform duration-300 hover:scale-105 ${className}`}
    src="/profilePic_draw.png"
    alt="ssanjua profile picture"
/>